# Exercícios: Aula 06 - Implementação de Servidor ⚙️

### 🟢 Nível Básico

1.  Qual o comando para instalar o SDK oficial de MCP no Node.js?
2.  Por que não devemos usar `console.log` para mensagens de erro em um servidor MCP Stdio?

### 🟡 Nível Intermediário

3.  Explique a importância da propriedade `.describe()` ao definir uma ferramenta com Zod ou Python.
4.  O que é o **MCP Inspector** e como ele auxilia no desenvolvimento de servidores?

### 🔴 Nível Desafio

5.  **Codificação**: Escreva o trecho de código (em Node ou Python) para registrar uma Tool chamada `convert_currency` que recebe um valor (`amount`), uma moeda de origem (`from`) e uma de destino (`to`). Não precisa implementar a lógica real, apenas a estrutura de registro da Tool.